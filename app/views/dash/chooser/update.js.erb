<% if @success %>
  var callback = parent.$.fancybox.getInstance().$lastFocus.data('callback');
  parent[callback](<%= @asset.id%>);
<% else %>
$("#asset-form").html("<%= j(render(partial: "/dash/chooser/form", locals: {asset: @asset}))%>");
<% end %>